# tuple(): Convert an iterable (like a list) into a tuple.

nameList = ["apple", "mango", "grapes"]
print("The list elements are:", nameList)

nameTuple = tuple(nameList)  # Convert list to tuple
print("The tuple elements are:", nameTuple)
print()
