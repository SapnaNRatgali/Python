# pop(): Removes and returns the item at the specified index , (or the last item if no index is specified).

listing = [12,35,46,58,25,64]
print(listing)

listing.pop(2)
print("\nList after popping the element at index 2:")
print(listing)
